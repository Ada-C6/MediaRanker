require 'test_helper'

class HomeControllerTest < ActionController::TestCase
    # test "should show the index view and assign all three instance variables" do
    #     get :index
    #     assert_template 'home/index'
    #
    #     albums = assigns(albums(:another_album))
    #     assert_not_nil albums
    #
    #     books = assigns(books(:another_book))
    #     assert_not_nil books
    #
    #     movies = assigns(movies(:another_movie))
    #     assert_not_nil movies
    # end
    #
    # The above test fails--I tried two different thing and searched google, still don't understand why.
end
