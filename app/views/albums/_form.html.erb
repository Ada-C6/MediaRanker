<% if @album.errors.any? %>
<ul>
  <% @album.errors.each do |column, message| %>
  <li>
    <%= column %>
    <%= message %>
  </li>
  <% end %>
</ul>
<% end %>

<%= form_for @album, method: action_name do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :artist %>
<%= f.text_field :artist %>
<%= f.label :description %>
<%= f.text_field :description %>
<%= f.submit "Submit!" %>
<% end %>
