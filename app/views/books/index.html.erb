<h1>Books</h1>
<% @books.each do |book| %>
<h2><%= link_to(book.title, book_path(book.id)) %></h2>
<p><%= button_to 'UpVote', upvote_book_path(book.id), method: :post %></p>
<p><%= book.rank %></p>
<% end %>
