<h2><%= @book.name %></h2>

<p>Written by: <%= @book.author %></p>

<p>Ranked: <%= @book.ranked %></p>

<p>Description:<%= @book.description %></p>

<p><%= button_to "Upvote", upvote_book_path(@book.id), :method => :patch %></p>

<ul>
  <li><%= link_to "Edit '#{@book.name}'", edit_book_path(@book.id) %></li>
  <li><%= link_to "Delete", book_path(@book.id), method: :delete, data: { confirm: "Are you sure you want to delete?"} %></li>
  <li><%= link_to "View All Books", books_path %></li>
  <li><%= link_to "View All Media", home_path %></li>
</ul>
