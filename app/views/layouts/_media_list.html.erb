  <ol>
    <% display_amount(media_collection).times do |i| %>
    <% media = media_collection[i] %>
    <li>
      <h2><%= link_to media.name, create_media_path(media) %></h2>
      <small><%= "Ranked: #{media.upvotes}" %></small>
    </li>

    <% end  %>
  </ol>
