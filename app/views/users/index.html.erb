<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>
</p>Once functional. I will delete all this.</p>
<hr />
<h1>MAIN PAGE</h1>
<p>This is page that is going to link to the other pages.</p>
<hr />

<p>Reveal all movies first. Then you can limit what shows on users main page.</p>

<article>
  <h1>Top Movies/All Movies</h1>
    <% @movies.each do |movie| %>
    <h1><%= link_to(movie.name, movie_path(movie.id)) %>
    <%= "Ranked: " %>
    <% end %>
  </h1>
</article>

<article>

   <h1>Top Books</h1>
   <% @books.each do |book| %>
   <h1><%= link_to(book.name, book_path(book.id)) %>
   <% end %>
   </h1>

</article>
