<!-- <h1><%= @movie.title %></h1>
<h3>Directed by: <%= @movie.director %></h3>
<p><%= @movie.description %></p>
<p>Ranked: <%= @movie.rank %></p>

<%= button_to "Edit Movie", movie_edit_path(@movie.id), method: :get %>
<%= button_to "Delete Movie", movie_delete_path(@movie.id), method: :delete, data: {confirm:"Are you Sure?" }  %>
<%= button_to 'Upvote', movie_upvote_path(@movie.id), method: :post %>

<%= link_to 'Show All Media', index_path %> -->

<section class="record">
  <h1><%= @movie.title %></h1>
  <h4>By: <%= @movie.director %></h4>
  <p><%= @movie.description %></p>
  <p class="rank">Ranked: <%= @movie.rank %></p>

  <%= button_to 'Upvote', movie_upvote_path(@movie.id), method: :post, class: 'show_button upvote'  %>
  <%= button_to "Edit Movie", movie_edit_path(@movie.id), method: :get, class: 'show_button edit' %>
  <%= button_to "Delete Movie", movie_delete_path(@movie.id), method: :delete, class: 'show_button delete', data: {confirm:"Are you Sure?" }  %>

</section>

<section class="links">
  <%= link_to 'Show All Media', index_path %>
  <%= link_to 'Show All Movies', movie_index_path %>
</section>
