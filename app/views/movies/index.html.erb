<table>
  <thead>
    <tr>
      <th>
        <p>Ranking</p>
      </th>
      <th>
        <p>Name</p>
      </th>
      <th>
        <p>Upvote</p>
      </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <% @movies.each do |m| %>
        <td><%= "#{ m.upvotes }" %></td>
        <td><%= link_to "#{ m.name }", movie_path(m) %></td>
        <td><%= button_to "Upvote", action: :vote, id: m.id %></td>
    </tr>
      <% end %>
  </tbody>
</table>

<%= button_to "View All Media", root_path, method: :get %>
<%= button_to "Add a Movie", new_movie_path, method: :get %>
