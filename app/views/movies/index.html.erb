<h1>Movies</h1>
<% @movies.each do |movie| %>
<h2><%= link_to(movie.title, movie_path(movie.id)) %></h2>
<p><%= button_to 'UpVote', upvote_movie_path(movie.id), method: :post %></p>
<p>Rank: <%= movie.rank %></p>
<% end %>
